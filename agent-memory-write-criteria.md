# Agent 长期记忆写入准则 · 梳理版

> 来源：Agent Memory 系列课程（上集 2'30" + 下集 2'20"，共约 5'10"）
> 核心命题：**写得多 ≠ 记得好**——长期记忆写什么、不写什么，不能靠感觉，必须靠工程标准。

---

## 一、长期记忆只存三类高价值信息

| 类别 | 英文 | 定义 | 示例 |
|---|---|---|---|
| 偏好 | Preference | 用户长期稳定的偏好方向 | 偏好中文、简短回复 |
| 事实 | Facts | 用户确认过的客观信息 | 住北京、养狗 |
| 经验 | Experience | 能用于行动的知识 | 踩坑后总结的经验 |

**边界**：一次性信息（天气查询、临时快递号、随口寒暄）**不该进入长期记忆**，只会变成噪音。

---

## 二、写入长期记忆的五道关卡

| 关卡 | 名称 | 核心要求 |
|---|---|---|
| 01 写入准则 | 三层筛选 | Q1 一次性? Q2 跨会话复用? Q3 属三类? 任一否→不写 |
| 02 触发时机 | 按需触发 | 强信号才写（明确偏好/任务阶段结束/自评≥阈值），非每轮必写 |
| 03 抽取归一 | 去重合并 | 改主意→覆盖；同事实不同说法→合并；互补细粒度→并存 |
| 04 衰减淘汰 | 有出口 | TTL 到期 + 访问频次低/被否定→降级淘汰 |
| 05 隐私可控 | 强合规 | 敏感数据脱敏隔离+单独加密；用户可查/删/改/撤回 |

**判定规则**：落库写入是一条"强建模"，不是来者不拒——给写入加一条硬筛选门槛。

---

## 三、记忆生命周期

```
写入 → 短期热缓冲 → 长期记忆(永久) → 遗忘(淘汰，有出口)
```

记忆不是只进不出的仓库：长时间不被访问自动衰减，被反复否定的记忆优先淘汰。

---

## 四、上线后必盯的核心指标

| 指标 | 含义 | 关注点 |
|---|---|---|
| 写入量 | 总量 + 有效写入量 | 写得多但没人用 = 白写 |
| **命中率** | 被调用次数 / 总查询次数 | 记忆真正价值的体现 |
| 冲突率 | 前后不一致被判定的比例 | 高 = 抽取逻辑有 bug |
| 删除率 | 被用户删除/拒绝的数量 | 高 = 根本不该写进去 |
| 审计日志 | 写入时间/修改者/淘汰原因 | 复盘线上问题的"救命稻草" |

---

## 五、四大核心能力（一句话总结）

- **能判断**：三类信息归口筛选，五类信息归三类
- **能归一**：主题合并、冲突覆盖、去重归一
- **能衰减**：TTL 到期自动淘汰，有出口的记忆系统
- **能审计**：全链路日志，可追溯

> 把"记性"当"容量"，越胀大越混乱；**有出口的记忆系统，才扛得住真实业务。**

---

## 六、对照：我们当前的记忆架构

| 课程三类 | 本机对应层 | 说明 |
|---|---|---|
| 事实 Facts | 云端 profile（自动注入） | 用户长期稳定的客观信息 |
| 偏好 Preference | 用户级本地 MEMORY.md | 跨项目习惯、操作规范 |
| 经验 Experience | 工作区 memory/ | 项目事实、踩坑经验、技术决策 |

**可补强方向**（对照五道关卡）：
- 触发时机：目前偏"明确请求才写"，可加"关键任务阶段结束自动沉淀经验"的高门槛触发；
- 衰减淘汰：当前无 TTL/频次机制，旧日志靠人工蒸馏，可引入定期清理；
- 审计：写入来源与时间有记录，但缺"命中率/冲突率"量化复盘。
