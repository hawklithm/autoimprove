<!-- AUTOIMPROVE_START -->
## AutoImprove

AutoImprove learned rules from your past corrections; call `search_knowledge` before any task — analysis, file operations (read/edit/write/move/create), or code changes — to check them (<10ms).
If you are a Task/sub-agent, you do not receive MCP initialize instructions, so proactively call `search_knowledge` yourself.
After corrections, run `/autoimprove-summarize` to capture patterns for future sessions.
Run `./setup_claude.sh` again whenever this guidance needs to be refreshed.
<!-- AUTOIMPROVE_END -->
